# Javascript-Interview-Questions
Javascript interview question


# here we discuss about all basic javascript question

1. Basic fundamental Question Q1 to Q6
    # What is JavaScript? What is the role of JavaScript engine?
    # What are Client side and Server side? v. IMP.
    # What is Scope in JavaScript? v. IMP.
    # What is the type of a variable in JS when it is declared without using the
    # What is Hoisting in JavaScript? v. IMP.
    # What is JSON

2. Variables and datatype Q7 to Q12
    # What are variables? What is the difference between var, let, and const? v. IMP.
    # What are data types in JS?
    # What is the difference between primitive and non-primitive data types? v. IMP.
    # What is the difference between null and undefined in JS?
    # What is the use of typeof operator?
    # What is type coercion in JS?

3. Orators and Conditions Q13 to Q15
    # What is operator precedence?
    # What is the difference between unary, binary, and ternary operators?
    # What is short-circuit evaluation in JS?
    # What are the types of conditions statements in JS? IMP.
    # What is the difference between and == and === ?

4. Array Q16 to Q18
    # What is the difference between Spread and Rest operator in JS?
    # What is Array Destructuring in JS? v. IMP.
    # What are array-like objects In JS?


5. Loops
    # What is a loop? What are the types of loops in JS?
    # What is the difference between while and do-while loops?
    # What is the difference between break and continue statement?
    # What is the difference between for an for...of loop in JS

6. Function
    # What are Functions in JS? What are the types of function?
    # # What is the difference between named and anonymous functions? When to use what in applications?
    # # What is function expression in JS?
    # What are Arrow Functions in JS? What is it use? v. IMP.
    # strict mode implementation
    # What are Callback Functions? What is it use?
    # What is Higher-order function In JS?
    # In how many ways can you pass arguments to a function?
    # What are First-Class functions in JS?
    # What are Pure and Impure functions in JS?
    # What is Function Currying in JS?
    # What are call, apply and bind methods in JS?
    # What is the use of event handling in JS?
    # In JavaScript, default parameters allow you to specify default values for function parameters.

7. String
    # What are template literals and string interpolation in strings?
    # In ow many ways you can concatenate strings?
    # What are some important string operations in JS?
    # What is the difference between single quotes (''), double quotes("") & backticks (``)
    # In how many ways you can concatenate strings?
    # What are some important string operations in JS?
    # What is string immutability?

8. DOM
    # What is DOM? What is the difference between HTML and the DOM?
    # How do you select, modify, create and remove DOM elements?
    # What are selectors in JS?
    # Q. What is the difference between getElementByld, getElementsByClassName and getElementsByTagName?


    